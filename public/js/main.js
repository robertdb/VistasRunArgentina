function checkActual(){
  var pointer = $('.icon-nav');
  var id = $(pointer).attr('id');
  if (id === 'actual')
    /*$('#actual').css(,);
    $('#actual').css(,);*/
  console.log(id);
}
$(document).ready(function() {

  //checkActual();

});
